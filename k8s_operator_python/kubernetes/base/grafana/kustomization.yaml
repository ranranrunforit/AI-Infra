apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources: []

configMapGenerator:
  - name: trainingjob-dashboard
    files:
      - dashboard.json
    options:
      labels:
        grafana_dashboard: "1"
