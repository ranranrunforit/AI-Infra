apiVersion: v1
kind: Service
metadata:
  name: trainingjob-operator
  namespace: ml-training
  labels:
    app: trainingjob-operator
spec:
  selector:
    app: trainingjob-operator
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
    - name: metrics
      port: 9090
      targetPort: 9090
      protocol: TCP
  type: ClusterIP
