apiVersion: v1
kind: Service
metadata:
  name: trainingjob-operator
  namespace: ml-training
  labels:
    app: trainingjob-operator
spec:
  selector:
    app: trainingjob-operator
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
