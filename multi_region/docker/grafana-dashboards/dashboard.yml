apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: 'Multi-Region ML Platform'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
